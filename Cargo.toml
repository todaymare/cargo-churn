[package]
name = "cargo-churn"
version = "0.1.0"
edition = "2024"
description = "Measure total lines added and deleted across all commits in a git repository"

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
